apiVersion: v1
kind: Service
metadata:
  name: node-docker-timefreeze
  labels:
    app: node-docker-timefreeze
spec:
  type: NodePort
  selector:
    app: node-docker-timefreeze
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30081
